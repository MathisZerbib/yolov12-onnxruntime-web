# Local development secrets (copy to .dev.vars and fill in)
# These are required for the worker to run in local development

# Private key for the market operator role (64 hex chars, no 0x prefix)
MARKET_OPERATOR_PRIVATE_KEY=your_private_key_here_without_0x_prefix